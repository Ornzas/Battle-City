#pragma once

const int TILE_SIZE = 32;
const int MAP_WIDTH = 20;
const int MAP_HEIGHT = 20;
const int UI_WIDTH = 5;

const int SCREEN_WIDTH = (MAP_WIDTH + UI_WIDTH) * TILE_SIZE;
const int SCREEN_HEIGHT = MAP_HEIGHT * TILE_SIZE;